# Quick Start

```bash
pip install -r requirements.txt
bash scripts/run_trimming_smoke.sh
bash scripts/run_rsd_structure.sh
```

Outputs are written to:

```text
results/
```

For the larger trimming suite:

```bash
bash scripts/run_trimming_paper.sh
```
